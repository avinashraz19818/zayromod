.env
.env.bak
.env.*
!.env.example
backups/*.db
backups/*.sqlite
database/*.db-shm
database/*.db-wal

# Runtime data and user/credential material — never commit these.
database/*.db
database/*.db-*
backups/
builds/
uploads/
keystore/
.env*
!.env.example

# Local/runtime/private files
firebase-service-account.json
firebase-service-account*.json
node_modules/
builds/
uploads/
backups/
database/*.db
database/*.db-wal
database/*.db-shm
keystore/*.keystore
*.backup.*
*.before-*

firebase-service-account.json
firebase-service-account*.json
node_modules/
builds/
uploads/
backups/
database/*.db
database/*.db-wal
database/*.db-shm
keystore/*.keystore
*.backup.*
*.before-*

# Android template project — local build cruft (never commit)
android-project/.gradle/
android-project/app/.gradle/
android-project/app/.cxx/
android-project/app/build/
android-project/build/
